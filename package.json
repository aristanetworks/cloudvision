{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "clean": "learn run clean",
    "ci": "lerna bootstrap",
    "lerna-version": "lerna version --conventional-commits --exact --no-push --yes"
  }
}
